import React from 'react';

/**
 * Renders a basic button component
 * @returns {JSX} Button component
 */
const TestButton = () => {
  return (
    <div>
      <button type="button">DummyButton</button>
    </div>
  );
};

export default TestButton;
